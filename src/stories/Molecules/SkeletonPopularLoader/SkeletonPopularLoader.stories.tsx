import type { Meta, StoryObj } from "@storybook/react";
import SkeletonPopularLoader from "./index";

const meta: Meta<typeof SkeletonPopularLoader> = {
	component: SkeletonPopularLoader,
	title: "Molecules/SkeletonPopularLoader",
};

export default meta;
